# EMN DevOps initializer

- name: Provision Raspberry Pi Consoles
  hosts: pi
  gather_facts: true
  tasks:

    - import_role:
        name: apt
      tags: [ 'init','init-all', 'apt' ]
  
    - import_role:
        name: config
      tags: [ 'init','init-all', 'config' ]
  
  
  handlers:
     - import_tasks: ../handlers/main.yml

