# EMN DevOps initializer

- name: Provision Raspberry Pi Entertainment Consoles
  hosts: raspberries
  gather_facts: true
  tasks:

    - import_role:
        name: apt
      tags: [ 'init','init-all', 'apt' ]
  
    - import_role:
        name: config
      tags: [ 'init','init-all', 'config' ]
  
    - import_role:
        name: autostart
      tags: [ 'init-all','init-all', 'autostart' ]

    - import_role:
        name: fs
      tags: [ 'init-all', 'fs' ]
      when: fs_mounts is defined
    
    - import_role:
        name: nfs
      tags: [ 'init-all', 'nfs' ]
      when: nfs_shares is defined or nfs_mounts is defined
  
    - import_role:
        name: k3s
      tags: [ 'init-all', 'k3s' ]
      when: install_k3s|bool
  
  handlers:
     - import_tasks: ../handlers/main.yml

